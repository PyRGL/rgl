version: 2

build:
   os: ubuntu-22.04
   tools:
      python: "3.8"

sphinx:
  configuration: doc/source/conf.py  # Adjust the path to your conf.py accordingly

python:
   install:
      - requirements: doc/requirements.txt
      - method: setuptools
        path: .

formats: []